{
    "name": "copa-calo",
    "displayName": "Copa Calo",
    "version": "0.0.0",
    "scripts": {
        "build": "npm --prefix functions run build",
        "serve": "npm run build && npx firebase serve"
    },
    "dependencies": {
        "firebase": "^7.8.2"
    },
    "devDependencies": {
        "firebase-tools": "^7.13.1"
    }
}